#include <stdlib.h>
#include <stdio.h>

int main() 
{
	int limit;
	limit = 30;

	int x;
	int z;
	int i;
	int q;
	for(i=0;i<9;i++) {
		for(z=0;z<9;z++) {
			for(x=0;x<9;x++){
				int n = 20;
				sprintf(t, 1, "%d", i);
				sprintf(y, 1, "%d", z);
				sprintf(u, 1, "%d", x);
				printf("%s", fin);
}
}
}	
}
